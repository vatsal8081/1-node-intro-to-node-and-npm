// 2
// in node we use commonJs module system the way it works and all other deep implimantation we will learn in future but just keep in mind for now that we have a module global object avalable which gives export to us and we can do export with the help of it

module.exports = () => console.log('hii from logger module....');